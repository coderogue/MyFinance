services:
  my-finance-system:
    build:
      context: ./my-finance-system
    container_name: my-finance-system
    environment:
      DATABASE_URL: file:/data/finance.db
      NODE_ENV: production
    ports:
      - "3001:3000"
    volumes:
      - ./my-finance-system/data:/data
    restart: unless-stopped
